#include <stdio.h>
int main(){
    float coef_x[2];
    float coef_y[2];
    float val_igual[2];
    float calculo_1;
    float y,x;
    float numerador;
    float denominador;
    
    for (int i = 0; i < 2; i++){
        scanf("%f", &coef_x[i]);
        scanf("%f", &coef_y[i]);
        scanf("%f", &val_igual[i]);
    }
    numerador = x 
return 0;
}